"use client";

import React from "react";
import { Target, Zap, TrendingUp, ChevronRight, Lock } from "lucide-react";

const GAP_SOLUTIONS = [
  {
    gap: "Cash Flow Leakage",
    solution: "The Debt-Exit Framework",
    benefit: "Address inefficiencies. We show you how to reorganize payments to prioritize high-interest debt while maintaining financial stability.",
    icon: <Zap className="w-6 h-6 text-brand-emerald" />,
  },
  {
    gap: "Asset Misallocation",
    solution: "The Smart Invest Tool",
    benefit: "Stop guessing. Our research-backed allocation matrix optimizes your portfolio based on actual risk-to-reward ratios.",
    icon: <TrendingUp className="w-6 h-6 text-brand-emerald" />,
  },
  {
    gap: "Growth Stagnation",
    solution: "Wealth Accelerator",
    benefit: "Move beyond basic savings. Leverage advanced asset classes and high-yield strategies to exponentially increase net worth.",
    icon: <Target className="w-6 h-6 text-brand-emerald" />,
  },
];

export default function ValueStack() {
  return (
    <section className="px-6 py-24 max-w-7xl mx-auto">
      <div className="text-center mb-16">
        <h2 className="text-3xl md:text-5xl font-extrabold mb-6 text-white">
          Closing the <span className="text-brand-emerald">Wealth Gap</span>
        </h2>
        <p className="text-slate-400 max-w-2xl mx-auto">
          Most financial plans fail because they don't address the specific leaks in your system. 
          Our tools are designed to plug those holes permanently.
        </p>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
        {GAP_SOLUTIONS.map((item, index) => (
          <div 
            key={index}
            className="group relative p-8 rounded-3xl bg-slate-900/50 border border-slate-800 hover:border-brand-emerald/40 transition-all duration-300 flex flex-col"
          >
            <div className="mb-6 p-3 w-fit rounded-2xl bg-brand-navy-dark border border-slate-700 group-hover:border-brand-emerald/50 transition-colors">
              {item.icon}
            </div>
            <h3 className="text-xl font-bold text-white mb-4 group-hover:text-brand-emerald transition-colors">
              {item.solution}
            </h3>
            <p className="text-slate-400 text-sm leading-relaxed mb-6">
              {item.benefit}
            </p>
            <div className="mt-auto flex items-center justify-between text-xs font-bold text-brand-emerald uppercase tracking-widest opacity-60 group-hover:opacity-100 transition-opacity">
              <span>Strategic Solution</span>
              <ChevronRight className="w-4 h-4" />
            </div>
          </div>
        ))}
      </div>

      <div className="mt-20 p-8 md:p-12 rounded-3xl bg-gradient-to-br from-brand-emerald/10 via-transparent to-transparent border border-brand-emerald/20 text-center max-w-4xl mx-auto relative overflow-hidden">
        <div className="relative z-10 space-y-4">
          <Lock className="w-12 h-12 text-brand-emerald mx-auto mb-4" />
          <h3 className="text-2xl font-bold text-white">Secure Your Financial Future</h3>
          <p className="text-slate-400 max-w-md mx-auto text-sm">
            All our blueprint tools are encrypted and delivered via secure digital portals 
            to ensure your financial strategy remains private.
          </p>
        </div>
        <div className="absolute -bottom-10 -right-10 w-40 h-40 bg-brand-emerald/10 blur-3xl rounded-full" />
      </div>
    </section>
  );
}
